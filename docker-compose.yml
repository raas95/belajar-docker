version: "3.7"
services:
  belajar-docker:
    image: nginx
    volumes:
    - ./page:/etc/nginx/templates
    ports:
    - "1234:80"
    environment:
    - NGINX_HOST=foobar.localhost
    - NGINX_PORT=80