version: "3.7"
services:
  belajar-docker:
    image: nginx
    volumes:
    - ./page:/etc/nginx/templates
    ports:
    - "8080:80"
    environment:
    - NGINX_HOST=foobar.localhost
    - NGINX_PORT=80