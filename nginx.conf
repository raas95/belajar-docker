http {
  # Existing server blocks...

  server {
    listen 80;
    server_name example.awek;
    root /usr/share/nginx/html;
    index index.html index.htm;

    # Other configuration directives...
  }
 
}